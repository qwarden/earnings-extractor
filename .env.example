# Required: Anthropic API key for Claude extraction
ANTHROPIC_API_KEY=sk-ant-...

# Optional: if set, the app requires a password to log in
APP_PASSWORD=changeme

# Optional: secret for signing session cookies (defaults to APP_PASSWORD if unset)
# Use a long random string in production: openssl rand -base64 32
APP_SECRET=random-secret-string

# Optional: max PDF upload size in MB (default: 20)
NEXT_PUBLIC_MAX_FILE_SIZE_MB=20

# Optional: Claude model to use for extraction (default: claude-sonnet-4-20250514)
# CLAUDE_MODEL=claude-sonnet-4-20250514
